# Optional shell environment configuration (no credentials required).
# The server binds to localhost by default; this is a personal research app.
LAYA_DEVICE=cuda:0
LAYA_CPU_THREADS=4
USE_TF=0
TOKENIZERS_PARALLELISM=false
